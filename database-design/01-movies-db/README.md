# Movies DB — Off-Platform Project
Codecademy "Design Databases with PostgreSQL" project. Covers CREATE TABLE, INSERT,
SELECT with WHERE, ALTER TABLE (adding columns + constraints), UPDATE (backfilling),
and UNIQUE constraints.