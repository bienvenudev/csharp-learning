# SQL

SQL and database work from the training curriculum.

## Structure

SQL fundamentals exercises as `.sql` files, each with comments explaining what the
query does. Codecademy provided the underlying table/data via their platform (not included
here) — a schema screenshot is included for reference where relevant.